import { BookOpen } from 'lucide-react';

export function PlaceholderPage() {
  return (
    <div className="placeholder">
      <BookOpen size={28} />
      <h1>Module available from the sidebar</h1>
      <p>This screen is connected and ready for the clinic workflow.</p>
    </div>
  );
}
