{
  "name": "curaclinic-auth-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Authentication-only backend for CuraClinic: Supabase-backed signup and OTP (Nodemailer) login. Handles nothing else — no clinic data, no app APIs.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.14"
  }
}
